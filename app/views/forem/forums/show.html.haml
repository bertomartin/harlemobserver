.forum
  %div.t-create-post.text-right
    = link_to "Create post", forem.new_forum_topic_path(@forum), :class => "btn btn-primary"
  / # = render :partial => 'forem/forums/head', :locals => { :forum => @forum }
  .panel.panel-default
    .panel-heading
      %h4 Current Discussions
    = forem_pages_widget(@topics)
    %table.topics.table-striped.table{:cellspacing => "0"}
      %thead
        %tr
          %th.icon
          %th.byline= t('forem.topic.headings.topic')
          %th.latest-post.text-center= t('forem.topic.headings.latest')
          %th.posts-count.text-center= t('forem.topic.headings.posts')
          %th.views-count.text-center= t('forem.topic.headings.views')
      %tbody
        - if @topics.empty?
          %tr
            %td{:align => "center", :colspan => "5"}= t('forem.topic.none')
        = render @topics
    / # = forem_pages_widget(@topics)
